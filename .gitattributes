# 데이터 산출물은 바이트 그대로 보존한다.
# 줄바꿈을 변환하면(Windows CRLF ↔ Linux LF) 같은 파일인데도 sha256이 달라져,
# 리포트 지문 가드(src/report/daily_report.py --check)가 플랫폼마다 다르게 판정한다.
data/** -text

# 바이너리 산출물 — diff/머지 대상 아님
*.parquet binary
*.joblib binary
*.db binary
*.h5 binary
*.pptx binary
